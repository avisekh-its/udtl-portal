{
  "name": "udtl",
  "version": "0.0.1",
  "private": true,
  "description": "UDTL Customer Portal & Operations Console — monorepo (web + worker + shared db schema)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @udtl/web",
    "dev:worker": "npm run dev -w @udtl/worker",
    "build": "npm run build -w @udtl/db && npm run build -w @udtl/web && npm run build -w @udtl/worker",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run generate -w @udtl/db",
    "db:migrate": "npm run migrate -w @udtl/db",
    "db:studio": "npm run studio -w @udtl/db"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
